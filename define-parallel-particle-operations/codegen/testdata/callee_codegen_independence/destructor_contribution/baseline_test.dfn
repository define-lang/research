define the potential action<my.domain.com:my_lib:/test> {
    it happens when {
        this particle is created.
    } and it does {
        define the position<box> {
            it may only contain particles where {
                it has the action</callee>.
            }
        }
        define the position<source>.
        create a particle in position<box>.
        create a particle in position<source>.
        move the particle in position<source> to position<box>::action</callee>::position<target>.
        create a particle in position<box>::action</callee>::position<run>.
        destroy the particle in position<box>::action</callee>::position<run>.
    }
}
