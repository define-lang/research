# Reference Graph Validator Test Instructions

- Before adding, removing, renaming, or updating a data-backed test, read and
  follow `define/testdata/AGENTS.md`.
- Never filter diagnostics by type or file. Assert on the exact diagnostics
  returned.
