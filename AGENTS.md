# Research archive

Preserve recorded results and source snapshots.

- Run Python with `uv run --frozen`.
- Install hooks: `uv run --frozen pre-commit install`.
- Run checks: `uv run --frozen pre-commit run --all-files`.
- Build: `bazelisk build //...`.
- Test: `bazelisk test //...`.
