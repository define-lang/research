"""Generated by the Define compiler."""

from define.runtime import literal
{% if trace_operations %}
from define.runtime import tracing
{% endif %}

import {{ entry_reference.module_name }}


def main():
{% if trace_operations %}
    scheduler = tracing.TracingScheduler()
    literal.start({{ entry_reference.module_name }}.{{ entry_reference.class_name }}, scheduler)
    tracing.write_operation_dependencies(scheduler.operation_dependencies)
{% else %}
    literal.start({{ entry_reference.module_name }}.{{ entry_reference.class_name }})
{% endif %}


if __name__ == "__main__":
    main()
